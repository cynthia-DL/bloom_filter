#include "filter.h"

int main() {
    int m = 8, i, k = 3;
    /*bitarray *a = create_bitarray(m);*/

    srand(time(NULL));

    /*set_bitarray(a, 7);
    set_bitarray(a, 0);
    set_bitarray(a, 3);
    set_bitarray(a, 4);
    reset_bitarray(a, 4);
    print_bitarray(a);
    clear_bitarray(a);
    printf("\n");
    print_bitarray(a);
    free_bitarray(a);*/

    /*filter *f = create_filter(m, k);
    set_bitarray(f->bitarray, 7);
    set_bitarray(f->bitarray, 0);
    set_bitarray(f->bitarray, 3);
    print_bitarray(f->bitarray);
    for(i = 0; i < f->weight_height; i++)
        printf("\n%d", f->weight[i]);
    if(is_member_filter(f, "test"))
        printf("\nOK\n");
    else
        printf("\nNOP\n")


    free_filter(f)*/

    return 0;
}
